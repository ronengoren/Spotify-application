let form = document.getElementById('task-form');
form.innerHTML = "<%= j (render 'events/new') %>";
form.style.display = "block";
var hash = []

    $(document).ready(function() {
    console.log(hash)
            var newjs = $(".modal-body").attr('id')
            $("#start_date").val(newjs) 
            $("#end_date").val(newjs) 
            console.log(newjs)
                        });

      $("#submitEvent").on("click", function() {
                        var val = $('.span11').map(function() {
                            return $(this).val();
                        })
                        hash.push({ "start_date" : val[0], "end_date" : val[1], "description" : val[2] });
                        var abc = []
                        var get =  JSON.parse(localStorage.getItem('hash'));
                        abc = [get];
                        abc.push(hash);
                        localStorage.setItem('hash', JSON.stringify(abc));
                        console.log(JSON.stringify(abc));
                        var ifinclude = abc.includes(newjs);
                        console.log("dfhdfg" + ifinclude)
                        $("#musix").remove();
                        $("#myModal .modal-body").load(click, function() {
                            $("#myModal").modal({"backdrop": "static"});
                        });

                        console.log('close btn click on fail modal')
                    })

function checkDated(){
console.log("yahooza")
}                    