console.log('Im hitting this file for the create action')
form.style.display = "none";
document.getElementById('my-tasks').insertAdjacentHTML('beforeend', '<%= j render partial: "events/show", locals: {event: @events} %>');
 $(document).ready(function() {
            var creatjs = $(".modal-body").attr('id')
            
            console.log("creatjs" + creatjs)
                        });
